[project]
name = "portfolio-api"
version = "1.0.0"
description = "API do portfólio"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.118.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic[email]>=2.10.0",
    "python-dotenv>=1.0.1",
    "resend>=2.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
